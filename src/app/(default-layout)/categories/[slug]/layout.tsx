export const metadata = {
    title: 'Category',
  }
  
  export default function LoginLayout({
    children
  }: {
    children: React.ReactNode,
  }) {
    return (
      <>
        {children}
      </>
    )
  }
