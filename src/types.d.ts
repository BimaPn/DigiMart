export type RegisterErrorsProps = {
    name ?: array,
    email ?: array,
    password ?: array,
    password_confirmation ?: array
}
export type UserProps = {
    id : number,
    username : string,
    email : string,
    password : string
}